"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateClaudeMd = generateClaudeMd;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const MARKER_START = '<!-- cgraph:start -->';
const MARKER_END = '<!-- cgraph:end -->';
function buildSection(graph) {
    const { stats, nodes, projectName } = graph;
    const topFiles = nodes
        .filter((n) => n.type === 'file')
        .slice(0, 20)
        .map((n) => `- ${n.name}`)
        .join('\n');
    const langList = Object.entries(stats.languages)
        .map(([lang, count]) => `${lang} (${count} files)`)
        .join(', ');
    const classes = nodes
        .filter((n) => n.type === 'class')
        .slice(0, 15)
        .map((n) => `- \`${n.name}\` in \`${n.filePath}\``)
        .join('\n');
    const functions = nodes
        .filter((n) => n.type === 'function')
        .slice(0, 20)
        .map((n) => `- \`${n.name}\` in \`${n.filePath}\` (line ${n.startLine})`)
        .join('\n');
    return `${MARKER_START}
## Code Intelligence (cgraph)

**Project:** ${projectName}
**Last analyzed:** ${graph.analyzedAt}
**Stats:** ${stats.totalFiles} files · ${stats.totalFunctions} functions · ${stats.totalClasses} classes
**Languages:** ${langList}

### Key Files
${topFiles || 'N/A'}

### Classes & Interfaces
${classes || 'N/A'}

### Top-level Functions
${functions || 'N/A'}

> This section is auto-generated by [cgraph](https://github.com/NSLog0/cgraph). Run \`cgraph analyze <path>\` to update.
${MARKER_END}`;
}
function generateClaudeMd(projectPath, graph) {
    const claudeMdPath = path_1.default.join(graph.projectPath, 'CLAUDE.md');
    const section = buildSection(graph);
    if (!fs_1.default.existsSync(claudeMdPath)) {
        fs_1.default.writeFileSync(claudeMdPath, `# ${graph.projectName}\n\n${section}\n`, 'utf-8');
        return;
    }
    let content = fs_1.default.readFileSync(claudeMdPath, 'utf-8');
    const startIdx = content.indexOf(MARKER_START);
    const endIdx = content.indexOf(MARKER_END);
    if (startIdx !== -1 && endIdx !== -1) {
        content = content.slice(0, startIdx) + section + content.slice(endIdx + MARKER_END.length);
    }
    else {
        content = content.trimEnd() + '\n\n' + section + '\n';
    }
    fs_1.default.writeFileSync(claudeMdPath, content, 'utf-8');
}
